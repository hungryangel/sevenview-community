# Contributing to SevenView

작은 재현과 검증 가능한 변경을 환영합니다.

## 가장 중요한 자료 정책

이 공개 저장소에는 다음 자료를 올릴 수 없습니다.

- 실제 환자 사진 또는 얼굴 스캔
- 환자명, 등록번호, 촬영일, 기관명이 드러나는 화면 캡처
- EXIF 개인정보가 남은 이미지
- 사용·공개 동의가 확인되지 않은 임상 자료

테스트와 문서는 Canvas 도형, 직접 만든 합성 이미지, 재배포가 명확히 허용된 fixture만
사용하세요. 이슈 재현에 임상 자료가 필요해 보이면 먼저 문제를 숫자 좌표나 합성 이미지로
축소하세요.

## 라이선스

기여는 저장소와 같은 GNU AGPL-3.0으로 받습니다. 기여한 코드는 이 저장소(무료 오픈코어) 안에서
쓰이며, VELNOC의 유료 확장 기능은 이 저장소 밖에서 따로 개발합니다.

## 개발 흐름

1. 이슈에서 문제와 성공 조건을 좁힙니다.
2. 동작 변경은 실패하는 테스트를 먼저 추가합니다.
3. 가장 작은 구현으로 테스트를 통과시킵니다.
4. `pnpm check`와 관련 E2E를 실행합니다.
5. UI 변경은 375px, 768px, 1280px에서 키보드와 시각 상태를 확인합니다.

```bash
corepack enable
pnpm install --frozen-lockfile
pnpm check
pnpm exec playwright install chromium
pnpm test:e2e
```

## 코드 기준

- strict TypeScript를 유지합니다.
- `any`, non-null assertion, 무검증 type assertion을 사용하지 않습니다.
- 사진 분석, 좌표 계산, 내보내기 로직은 UI와 분리합니다.
- 원본 이미지 객체를 변형하지 않고 보정값을 별도로 보관합니다.
- 오류를 성공처럼 보이게 만드는 fallback을 추가하지 않습니다.
- 새 상태는 텍스트·아이콘·접근 가능한 이름으로 표현합니다.

## 임상 주장

근거 없는 정확도, 안전성, 규제 승인, 진단·치료 효용 주장을 문서나 UI에 추가하지 마세요.
임계값 변경에는 데이터 출처, 동의 범위, 평가 방법, 실패 사례가 함께 제시되어야 합니다.
